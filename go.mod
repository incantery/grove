// grove imports the standard library and nothing else: a program that
// wants the model of a repo's worktrees should not inherit a terminal
// UI, a multiplexer, or anything but git.
module github.com/incantery/grove

go 1.25
